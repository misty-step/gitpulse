# Grooming Plan — 2026-03-07

## Vision Shift

Old: B2C report generator with Convex/Clerk/Stripe web app
New: Intelligent agent over git history. CLI/TUI first. Agentic UX.

## Backlog Cleanup

- **83 issues closed** — all pre-rebuild (Convex/Clerk/web-first architecture)
- **11 issues kept** — post-rebuild, still relevant (#162-#173)
- **8 issues created** — new agentic foundation (#174-#181)
- **2 issues closed post-review** — #177 (NL time parsing: LLM handles this), #180 (proactive insights: LLM reasons over data, don't pre-compute)
- **Total open: 17 issues**

## Themes Explored

1. **Legacy Purge** — 185 dead files from pre-rebuild era → #174
2. **DX Foundation** — lint/format, coverage, CI gaps → existing #169, #171, #173
3. **Agent Toolbox** — expand from 2 to 8 tools → #175, #176
4. **CLI-First UX** — natural time, local repo, standup, config → #177, #178, #179, #181
5. **Proactive Intelligence** — bus factor, bottlenecks, anomalies → #180

## Issues Created

- #174: [P0] Delete 185 legacy files (effort/m)
- #175: [P1] Expand agent toolbox 2→8 tools (effort/xl)
- #176: [P1] Enrich GitHub data: review state, diff stats, issues (effort/m)
- ~~#177: [P1] Natural language time parsing~~ CLOSED — agent-forward violation
- #178: [P1] Auto-detect local git repo (effort/s)
- #179: [P1] Standup command (effort/m)
- ~~#180: [P1] Proactive intelligence~~ CLOSED — LLM generates insights from rich data
- #181: [P2] Persistent CLI config (effort/m)

## Deferred

- **Streaming CLI output** — meaningful only after toolbox is built; revisit after #175
- **Interactive TUI/REPL** — requires session memory; revisit Q2
- **CI/CD data source (GitHub Actions)** — completes DORA metrics; revisit after #176
- **Local git history (git log)** — differentiator vs SaaS; revisit after #178
- **Digest/cron command** — retention multiplier; revisit after #179 (standup)
- **CODEOWNERS governance** — enterprise feature; later

## Research Findings

- **Tool registration pattern**: `tool()` from Vercel AI SDK with Zod inputSchema + async execute. Add to tools object in agent.ts:147-210.
- **GitHub API already returns**: review state (APPROVED/CHANGES_REQUESTED), PR state (open/closed/draft), PR diff stats (additions/deletions/changed_files). Collector ignores all of it.
- **ActivityEventSchema** already defines additions/deletions/filesChanged fields but collector never populates them.

## Agent-Forward Correction (mid-session)

Closed #177 and #180 after realizing they violated agent-forward design:
- **#177 (chrono-node time parsing)**: The agent already has an LLM. "Last week" is model territory. Fix: make --from/--to optional, let agent infer window from question.
- **#180 (deterministic insight functions)**: Pre-coding `detectBusFactor()` etc. limits the agent to insights we pre-imagined. Fix: give the model rich data (#175, #176), improve system prompt, let it reason.

Codified Agent-Forward Design in: global CLAUDE.md (Red Line), repo CLAUDE.md (Invariants), agentic-ui-contract skill (Anti-Patterns + Capability Test).

## Recommended Execution Order

1. #174 — Legacy purge (unblocks clean agent work)
2. #169 — Lint/format toolchain (DX foundation)
3. #178 — Local repo detection (quick win, improves all CLI work)
4. #176 — Enrich GitHub data (foundation for toolbox)
5. #175 — Agent toolbox expansion (core value delivery)
6. #179 — Standup command (daily-use hook)
