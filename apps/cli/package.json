{
  "name": "@gitpulse/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "gitpulse": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "bun build src/index.ts --outfile dist/gitpulse --target bun",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@gitpulse/agent-core": "workspace:*",
    "@gitpulse/schemas": "workspace:*"
  }
}
